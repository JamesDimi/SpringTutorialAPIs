package com.ddimi.SpringTutorialAPIs.topics

import spock.lang.Specification

class TopicControllerSpec extends Specification {

//    def "Div of (10/5) = 2 and response status is 200"() {
//        expect: "Div status is 200 and the correct result is returned"
//        myMock.perform(get("/calc/div/10/5"))
//                .andDo(MockMvcResultHandlers.print())
//                .andExpect(status().isOk())
//                .andExpect(jsonPath("div").value(2))
//    }
//
//    def "Mux of (3*3) = 9 and response status is 200"() {
//        expect: "Mux status is 200 and the correct result is returned"
//        myMock.perform(post("/calc/mux")
//                .contentType(MediaType.APPLICATION_JSON)
//                .content("{\"a\":3,\"b\":3}"))
//                .andExpect(status().isOk())
//                .andExpect(jsonPath("mux").value(9))
//    }
}
